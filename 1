# 1. Найти максимальный элемент диагонали матрицы
# 2. Вычислить количество отрицательных элементов под главной диагональю матрицы
a1 = int(input("Укажите размер матрицы: ")) # просим указать размер матрицы
from random import randint # импортируем библиотеку рандом
# указываем параметры рандома: диапазон чисел от -100 до 100 и размер указанный выше:
List_a = [[randint(-100, 100) for j in range(a1)] for i in range(a1)]
print() # печатаем пустую строку для удобного восприятия
for d in List_a: # для удобного отображения матрицы создаем цикл d
    print(d) # выводим созданную выше матрицу на экран
List_b = [] # создаем пустой список для добавления всех значений по диагонали матрицы
List_c = [] # создаем пустой список для добавления значений ниже главной диагонали матрицы
for J in range(len(List_a)): # создаем цикл, чтобы перебрать все значения матрицы
    for j in range(1): # создаем цикл, чтобы отобрать значения в матрице по диагонали
        List_b.append(List_a[J][J]) # добавляем в список отобранные выше значения
        for R in range(0, J): # создаем цикл, чтобы отобрать значения ниже главной диагонали матрицы
            List_c.append(List_a[J][R]) # добавляем в список отобранные выше значения
print() # печатаем пустую строку для удобного восприятия
print("Элементы главной диагонали матрицы: ", List_b) # выводим все значения диагонали матрицы
print("1. максимальный элемент диагонали матрицы: ", max(List_b)) # находим максимальный элемент
print() # печатаем пустую строку для удобного восприятия
print("элементы ниже матрицы: ", List_c) # выводим все значения ниже диагонали матрицы
List_d = 0 # для подсчета элементов, используем переменную, изначально которая равно нулю
for T in List_c: # создаем цикл, чтобы перебрать все значения ниже диагонали матрицы
    if T < 0: # создаем условие для поиска отрицательных элементов
        List_d += 1 # считаем количество отрицательных элементов
# выводим все найденные отрицательные элементы, находящиеся ниже главной диагонали матрицы:
print("2. Количество отрицательных элементов под главной \n диагональю матрицы равно: ", List_d)
    



